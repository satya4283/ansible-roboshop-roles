- name: Installing python3 packages
  ansible.builtin.pip:
    name: "{{item}}"
    state: present
  loop:
  - python
  - gcc 
  - python3-devel

- name: Install dependencies
  ansible.builtin.pip:
    requirements: requirements.txt
    executable: pip3.9  
  args:
    chdir: /app